# Express / Render (secret; never expose these in Vite or commit real values)
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_SECRET_KEY=sb_secret_REPLACE_ME

# Optional: comma-separated browser origins. Leave blank during initial setup.
# CORS_ORIGIN=https://your-shibasteps.vercel.app

# Vite / Vercel (public backend URL; not a secret)
VITE_API_BASE=https://your-shibasteps-api.onrender.com
